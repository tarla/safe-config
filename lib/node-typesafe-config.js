// Generated by CoffeeScript 1.10.0
(function() {
  var fetch;

  fetch = function(file) {
    var config, i, keyValue, len, ref, tuple;
    config = {};
    file = (require('fs')).readFileSync(__dirname + "/application.conf", 'utf8');
    ref = file.split('\n');
    for (i = 0, len = ref.length; i < len; i++) {
      keyValue = ref[i];
      if (!(keyValue.match(/^./))) {
        continue;
      }
      tuple = keyValue.split('=');
      config[tuple[0]] = tuple[1][0] === '$' ? process.env[tuple[1]] : tuple[1];
    }
    return config;
  };

  module.exports = fetch('');

}).call(this);
